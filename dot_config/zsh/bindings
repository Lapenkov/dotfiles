#/* vim: set filetype=zsh : */

bindkey "^[[1;3C" forward-word
bindkey "^[[1;3D" backward-word

# Bind this function to a key, e.g., Alt-w
zle -N wrap_last_word_with_which
bindkey '^Xe' wrap_last_word_with_which
zle -N fzf_bazel_target_completion
bindkey '^Xb' fzf_bazel_target_completion
